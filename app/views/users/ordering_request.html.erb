<div class="left-column">
  <h2>Request a New Ordering Unit</h2>
  <% @product = Product.new unless @product %>
  <% form_tag send_ordering_request_user_url(@user, :account_id => @account.id, :product_id => @product) do %>
    <p>Please let the adminstrator know what ordering unit you would like to add and why:</p>
    <%= text_area_tag :text %><br />
    <%= submit_tag 'Send Now'%>
  <% end %>
</div>